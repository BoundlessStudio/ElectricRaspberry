{
  "schema": 1,
  "description": "Creates the url for Geocode based on the default address and the search term",
  "type": "completion",
  "completion": {
    "max_tokens": 500,
    "temperature": 0,
    "top_p": 0,
    "presence_penalty": 0.0,
    "frequency_penalty": 0.0
  },
  "input": {
    "parameters": [
      {
        "name": "input",
        "description": "String to search for. 2 characters will only match exact matching locations. 3 and more characters will perform fuzzy matching. The search string can be a location name or a postal code.",
        "defaultValue": ""
      }
    ]
  }
}